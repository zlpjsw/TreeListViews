<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zlp.treelistview" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FoldingListViewActivity"
            android:label="@string/title_activity_folding_list_view" >
        </activity>
        <activity
            android:name=".UnFoldingListViewActivity"
            android:label="@string/title_activity_un_folding_list_view" >
        </activity>
    </application>

</manifest>
